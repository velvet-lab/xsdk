<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <!-- SonarQube needs this, do not change -->
        <ProjectGuid>d49489f5-b855-41ff-8fab-685590a2b535</ProjectGuid>
    </PropertyGroup>

    <PropertyGroup>
        <Description>Core library for xSdk, providing common utilities and abstractions for plugin development.</Description>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Newtonsoft.Json" PrivateAssets="contentfiles;analyzers;build;compile" />
        <PackageReference Include="NLog.Extensions.Logging" PrivateAssets="contentfiles;analyzers;build;compile" />
        <PackageReference Include="NLog.Targets.Network" PrivateAssets="contentfiles;analyzers;build;compile" />
        <PackageReference Include="OpenTelemetry" PrivateAssets="contentfiles;analyzers;build;compile" />
        <PackageReference Include="Spectre.Console.Cli" PrivateAssets="contentfiles;analyzers;build;compile" />
        <PackageReference Include="Weikio.PluginFramework" PrivateAssets="contentfiles;analyzers;build;compile" />
        <PackageReference Include="Zio" PrivateAssets="contentfiles;analyzers;build;compile" />
    </ItemGroup>

    <ItemGroup>
      <PackageReference Include="OpenTelemetry.Instrumentation.Process" VersionOverride="0.5.0-beta.7" />
        <ProjectReference Include="..\..\xSdk.Plugin\src\xSdk.Plugin.csproj" />
    </ItemGroup>

</Project>
